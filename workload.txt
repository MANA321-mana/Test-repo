let arr = [1,1,3,4,6,7,5];
let n=7;

const noOfServer = ( arr, n) =>{
  let s = arr.sort((a,b)=>a - b);
let arr = [1,3,1,6,4,7];
let n=7;
const noOfServer = ( arr, n) =>{
  arr.sort((a,b)=>a - b);
  let l = arr.length;
  console.log(arr)
  let arr1=[];
  for(var i =l-1,j=0 ; i >= j ;i--){
    if(arr[i] + arr[j] <= n && arr[i] !== arr[j]){
      arr1.push([arr[i],arr[j]]);
      j++;
    }else{
      arr1.push([arr[i]]);
    }
  }
  return arr1;
}

console.log(noOfServer(arr,n));